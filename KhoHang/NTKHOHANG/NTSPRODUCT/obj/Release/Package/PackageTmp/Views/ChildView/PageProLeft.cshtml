@model IList<NTSPRODUCT.Models.Support>
@{
    var allPro = (List<NTSPRODUCT.Models.Product>)ViewBag.allPro;
    string typePage = ViewBag.typePage;
}
<div class="col-md-3 col-xs-12 col-lg-3">
    @if (typePage.Equals("list"))
    {
        <div class="aside_catalog">
            <div class="title_module">
                <h2><span>Danh mục sản phẩm</span></h2>
            </div>
            <div class="categories-box">
                <ul class="nav navbar-pills">
                    @foreach (var item in allPro)
                    {
                        <li class="nav-item ">
                            <a href="/san-pham/@item.pro_key" title="@item.pro_name" class="nav-link">@item.pro_name</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
        <div class="aside_support support_online">
            <div class="title_module">
                <h2><span>@ClassExten.GetLangKey("key_httt")</span></h2>
            </div>
            <div class="support_content">
                <div class="list_support">
                    <ul>
                        @foreach (var item in Model)
                        {
                            if (item.sType == 1)
                            {
                                <li><a href="skype:@item.nick?chat" title="@item.fullName"><span class="ico"><i class="fa  fa-skype"></i></span><span class="title_">@item.fullName</span></a></li>
                            }
                            else if (item.sType == 2)
                            {
                                <li><a href="tel:@item.phone" title="@item.fullName"><span class="ico"><i class="fa  fa-phone"></i></span><span class="title_">@item.fullName: @item.phone</span></a></li>
                            }
                            else
                            {
                                <li><a href="@("http://zalo.me/0981481368:"+item.nick)" title="@item.fullName"><span class="ico"><i class="fa  fa-phone"></i></span><span class="title_">@item.fullName</span></a></li>
                            }
                        }
                    </ul>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="aside_support support_online">
            <div class="title_module">
                <h2><span>@ClassExten.GetLangKey("key_httt")</span></h2>
            </div>
            <div class="support_content">
                <div class="list_support">
                    <ul>
                        @foreach (var item in Model)
                        {
                            if (item.sType == 1)
                            {
                                <li><a href="skype:@item.nick?chat" title="@item.fullName"><span class="ico"><i class="fa  fa-skype"></i></span><span class="title_">@item.fullName</span></a></li>
                            }
                            else
                            {
                                <li><a href="tel:@item.phone" title="@item.fullName"><span class="ico"><i class="fa  fa-phone"></i></span><span class="title_">@item.fullName: @item.phone</span></a></li>
                            }
                        }
                    </ul>
                </div>
            </div>
        </div>
        <div class="aside_catalog">
            <div class="title_module">
                <h2><span>Danh mục sản phẩm</span></h2>
            </div>
            <div class="categories-box">
                <ul class="nav navbar-pills">
                    @foreach (var item in allPro)
                    {
                        <li class="nav-item ">
                            <a  href="/san-pham/@item.pro_key" title="@item.pro_name" class="nav-link">@item.pro_name</a>
                        </li>
                    }
                </ul>
            </div>
        </div>

    }

</div>
